events {}

http {
    server {
        include mime.types;
        listen 80;
        server_name localhost;

        location / {
            root /app;
            index index.html;
        }
    }
}
